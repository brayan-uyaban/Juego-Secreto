/*let listaGenerica = [];
let lenguajesDeProgramacion = ["JavaScrip", "C", "C++", "Kotlin", "Python"];

lenguajesDeProgramacion.push("Java", "Ruby", "GoLang");

mostrarLenguajes();

function mostrarLenguajes(){
  console.log(lenguajesDeProgramacion);
}

//mostrarLenguajes();
let reverso = lenguajesDeProgramacion.reverse();
console.log(reverso);
*/

/*
let numeros = [5, 8, 12, 3, 6];
let sumaLista = numeros.reduce();

function promedio(lista){
  let suma = lista.reduce();
  console.log(lista/5);
}

promedio();
*/


//Promedio
/*
let numeros = [1, 2, 3, 4];

// 0 + 1 + 2 + 3 + 4
let valorInicial = 0;
let sumaTotal = numeros.reduce(
  (acumulador, valor) => acumulador + valor,
);

console.log(sumaTotal);
console.log(sumaTotal/4);
*/

let numeros = [1, 2, 3, 4];
let numeroMayor = Math.max(numeros);